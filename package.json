{
  "name": "loop",
  "version": "1.0.0",
  "packageManager": "pnpm@10.28.0",
  "engines": {
    "node": ">=20.19 <26"
  },
  "main": "dist/main.js",
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "build:styles": "tailwindcss -i ./src/renderer/styles/tailwind.input.css -o ./src/renderer/styles/main.css --minify",
    "build:electron": "tsc -p tsconfig.build.json",
    "build:renderer": "vite build",
    "build": "pnpm clean && pnpm build:styles && pnpm build:electron && pnpm build:renderer",
    "start": "pnpm build && electron .",
    "dev": "pnpm build && electron .",
    "lint": "eslint . --max-warnings=0",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "test:e2e": "pnpm build && node tests/e2e/smoke-electron.test.mjs",
    "package:smoke": "pnpm build && node scripts/package-smoke.mjs",
    "check": "pnpm lint && pnpm typecheck && pnpm test && pnpm test:e2e && pnpm package:smoke"
  },
  "devDependencies": {
    "@electron/packager": "^19.1.0",
    "@eslint/js": "^10.0.1",
    "@types/node": "^25.5.0",
    "@vitest/coverage-v8": "^4.1.0",
    "electron": "^35.1.2",
    "eslint": "^10.0.3",
    "globals": "^17.4.0",
    "jsdom": "^28.1.0",
    "prettier": "^3.8.1",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.57.2",
    "vite": "^8.0.2",
    "vitest": "^4.1.0"
  },
  "dependencies": {
    "@elevenlabs/elevenlabs-js": "^2.36.0",
    "dotenv": "^17.3.1",
    "ffmpeg-static": "^5.3.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron",
      "ffmpeg-static"
    ]
  }
}
